package com.example.neptune.ui.commons

/**
 * An enum for the different types of track lists.
 */
enum class TrackListType {
    HOST_QUEUE, HOST_VOTE, HOST_SEARCH, PARTICIPANT_VOTE, PARTICIPANT_SEARCH
}